# Optional rmguard config (overrides defaults)
# 1 = active (default), 0 = disabled
# RM_GUARD=1

# Allowed toplevels (not blocked)
# Default: /tmp and /var/tmp
# You can add others if your operation requires it (careful!):
# ALLOW_TOPLEVEL="/tmp /var/tmp"
